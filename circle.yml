machine:
  services:
    - docker

dependencies:
  override:
    - docker pull f799/learning:UpdateSDK

test:
  override:
    - pwd
    - ls
    - echo "Comment"
    - mono --version
    - chmod +x build.sh
    - /bin/bash ./build.sh
